syntax = "proto3";

package ng;

message DeviceId {
  uint32 did = 1;
  uint32 cls = 2;
  uint32 sid = 3;
}

enum Endpoint {
  EndpointAppDefault = 0;
  EndpointApp1 = 1;
  EndpointApp2 = 2;
  EndpointBoardLevelControl = 3;
}