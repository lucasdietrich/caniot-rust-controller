syntax = "proto3";

package ng;

message DeviceIdInfos {
  uint32 did = 1;
  uint32 cls = 2;
  uint32 sid = 3;
}

message DeviceId { uint32 did = 1; }

enum Endpoint {
  EndpointAppDefault = 0;
  EndpointApp1 = 1;
  EndpointApp2 = 2;
  EndpointBoardLevelControl = 3;
}